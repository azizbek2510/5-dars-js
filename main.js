const name = prompt("Введите имя:");
const spaceId = prompt("Введите Space ID:");
const spacePassword = prompt("Введите Space Password:");

const userInfo = {
    name: name,
    ID: spaceId,
    password: spacePassword
};

console.log(userInfo);
